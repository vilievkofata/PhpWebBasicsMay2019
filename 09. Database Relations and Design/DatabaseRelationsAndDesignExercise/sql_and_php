CREATE DATABASE IF NOT EXISTS sql_and_php;
CREATE TABLE IF NOT EXISTS sql_and_php.students
(
    first_name VARCHAR(255)        NOT NULL,
    last_name  VARCHAR(255)        NOT NULL,
    number     INT                 NOT NULL,
    phone      BIGINT DEFAULT NULL NULL,
    CONSTRAINT students_pk
        PRIMARY KEY (number)
);